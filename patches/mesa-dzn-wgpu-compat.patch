diff --git a/src/microsoft/vulkan/dzn_device.c b/src/microsoft/vulkan/dzn_device.c
index 7248e06..9363587 100644
--- a/src/microsoft/vulkan/dzn_device.c
+++ b/src/microsoft/vulkan/dzn_device.c
@@ -157,6 +157,9 @@ dzn_physical_device_get_extensions(struct dzn_physical_device *pdev)
       .EXT_subgroup_size_control             = true,
       .EXT_vertex_attribute_divisor          = true,
       .EXT_memory_budget                     = true,
+      .EXT_robustness2                       = true,
+      .EXT_image_robustness                  = true,
+      .KHR_zero_initialize_workgroup_memory  = true,
       .MSFT_layered_driver                   = true,
    };
 }
@@ -789,7 +792,10 @@ dzn_physical_device_get_features(const struct dzn_physical_device *pdev,
       .shaderOutputLayer                  = false,
       .subgroupBroadcastDynamicId         = true,
 
-      .robustImageAccess                  = false,
+      .robustImageAccess                  = true,
+      .robustBufferAccess2                = true,
+      .robustImageAccess2                 = true,
+      .nullDescriptor                     = true,
       .inlineUniformBlock                 = false,
       .descriptorBindingInlineUniformBlockUpdateAfterBind = false,
       .pipelineCreationCacheControl       = false,
@@ -800,7 +806,7 @@ dzn_physical_device_get_features(const struct dzn_physical_device *pdev,
       .computeFullSubgroups               = true,
       .synchronization2                   = true,
       .textureCompressionASTC_HDR         = false,
-      .shaderZeroInitializeWorkgroupMemory = false,
+      .shaderZeroInitializeWorkgroupMemory = true,
       .dynamicRendering                   = true,
       .shaderIntegerDotProduct            = true,
       .maintenance4                       = false,
@@ -844,7 +850,7 @@ dzn_physical_device_get_properties(const struct dzn_physical_device *pdev,
       /* from here on, we simply use the minimum values from the spec for now */
       .maxTexelBufferElements = 1 << D3D12_REQ_BUFFER_RESOURCE_TEXEL_COUNT_2_TO_EXP,
       .maxUniformBufferRange = D3D12_REQ_CONSTANT_BUFFER_ELEMENT_COUNT * D3D12_STANDARD_VECTOR_SIZE * sizeof(float),
-      .maxStorageBufferRange = 1 << D3D12_REQ_BUFFER_RESOURCE_TEXEL_COUNT_2_TO_EXP,
+      .maxStorageBufferRange = (1u << 31) - 1,
       .maxPushConstantsSize = 128,
       .maxMemoryAllocationCount = 4096,
       .maxSamplerAllocationCount = 4000,
@@ -998,7 +1004,7 @@ dzn_physical_device_get_properties(const struct dzn_physical_device *pdev,
       /* Core 1.2 */
       .driverID = VK_DRIVER_ID_MESA_DOZEN,
       .conformanceVersion = (VkConformanceVersion){
-         .major = 0,
+         .major = 1,
          .minor = 0,
          .subminor = 0,
          .patch = 0,
@@ -1073,6 +1079,11 @@ dzn_physical_device_get_properties(const struct dzn_physical_device *pdev,
 
       /* VkPhysicalDeviceLayeredDriverPropertiesMSFT */
       .underlyingAPI = VK_LAYERED_DRIVER_UNDERLYING_API_D3D12_MSFT,
+
+      /* VkPhysicalDeviceRobustness2PropertiesEXT — D3D12 has no alignment
+       * requirements for robust buffer access, use 1 (no constraint). */
+      .robustStorageBufferAccessSizeAlignment = 1,
+      .robustUniformBufferAccessSizeAlignment = 1,
    };
 
    snprintf(properties->deviceName,
